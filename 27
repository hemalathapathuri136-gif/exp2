T = int(input())

for i in range(T):
    N, A, B, C = map(int, input().split())

    dishes = min(A, B) + min(C, B - min(A, B))

    if dishes >= N:
        print("YES")
    else:
        print("NO")
